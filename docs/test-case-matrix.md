# Test Case Matrix

The authoritative, up-to-date test case tracking lives in `Ambia_Automation_Tracker.xlsx`:

- **Maestro Coverage Matrix** — every test case Maestro can automate, with script name and stability tracking
- **Appium Backlog** — scenarios deferred to Appium, with reasoning
- **Feature Automation Tracker** — the same test cases grouped by app feature instead of phase

This file is a placeholder in case a lightweight markdown mirror is useful later (e.g. for quick reference in PRs). Not required to be kept in sync manually — the workbook is the source of truth.
